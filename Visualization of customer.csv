import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt

categories = pd.read_csv("categories.csv")
customers = pd.read_csv("customers.csv")
employees = pd.read_csv("employees.csv")

#Using the Dataset
print(customers.head())
print(customers.info())
print(customers.describe())
print(customers.isnull().sum())

#Graph of customer age distribution
plt.figure(figsize=(8,5))
sns.histplot(customers["age"], bins=10)
plt.title("Customer Age Distribution")
plt.xlabel("Age")
plt.ylabel("Number of Customers")

plt.show()

#Graph of gender distribution 
plt.figure(figsize=(6,4))
sns.countplot(data=customers,
              x="gender")
plt.title("Gender Distribution")
plt.show()

#Graph of Customers by city
plt.figure(figsize=(8,5))
sns.countplot(
    data=customers,
    x="city",
    order=customers["city"].value_counts().index)
plt.xticks(rotation=45)
plt.title("Customers by City")
plt.show()

#Pie Chart of Customers by city
city_count = customers["city"].value_counts()
plt.figure(figsize=(7,7))
plt.pie(
    city_count,
    labels=city_count.index,
    autopct="%1.1f%%"
)
plt.title("Customer Percentage by City")
plt.show()

#Boxplot opf Age
plt.figure(figsize=(7,4))
sns.boxplot(x=customers["age"])
plt.title("Customer Age Boxplot")
plt.show()

#Stats of Age
print("Average Age :", customers["age"].mean())

print("Maximum Age :", customers["age"].max())

print("Minimum Age :", customers["age"].min())

print("Median Age :", customers["age"].median())

#Correlation Heatmap of Customer ids and their Age
plt.figure(figsize=(6,4))
sns.heatmap(
    customers[["customer_id","age"]].corr(),
    annot=True,
    cmap="coolwarm"
)
plt.title("Correlation Heatmap")
plt.show()

#Employees list
print(employees.head())
print(employees.info())

#Bar Chart of Categories
plt.figure(figsize=(8,5))
sns.countplot(
    data=categories,
    y="category_name"
)
plt.title("Available Product Categories")
plt.show()
